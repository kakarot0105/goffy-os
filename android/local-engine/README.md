# Local Engine

The local engine currently uses anchored deterministic routes for Mac status,
phone battery status, and privacy-minimized device information. Additional
offline classifiers belong here only after the exact-rule baseline is measured
and covered by fail-closed tests.

Do not add a large local language model without real-device memory, startup, and
battery benchmarks.
