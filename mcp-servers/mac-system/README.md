# mac-system

Future standalone MCP transport adapter for read-only Mac status capabilities.
The current typed implementations live in:

- `hub/src/goffy_hub/tools/mac_system.py` for `mac.system_info`
- `hub/src/goffy_hub/tools/mac_processes.py` for `mac.processes.list`
