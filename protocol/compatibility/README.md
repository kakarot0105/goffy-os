# Protocol Compatibility

Milestone 0 supports exactly protocol version `0.1.0`. Unsupported versions are
rejected before tool payload parsing. A compatibility matrix and migration rules
will be added before a second protocol version is introduced.
