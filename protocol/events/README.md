# Protocol Events

Wire events use the schemas in `protocol/schemas` and the typed Python models in
`protocol/python/goffy_protocol`. Event persistence and Android serialization are
planned for Milestone 1.
